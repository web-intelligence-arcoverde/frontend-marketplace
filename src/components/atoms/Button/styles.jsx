import styled from 'styled-components';

export const CardButton = styled.div`
  width: 100%;
  display: flex;
  button {
    cursor: pointer;
    border: none;
    width: 100%;
    height: 48px;
  }
`;
